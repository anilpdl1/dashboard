import { AnimatePresence, motion } from 'framer-motion'
import { Code2, X } from 'lucide-react'
import { NavigationLinks } from './Sidebar'
export default function MobileNav({ open,onClose }) { return <AnimatePresence>{open && <><motion.div className="fixed inset-0 z-40 bg-slate-900/35 lg:hidden" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={onClose}/><motion.aside className="fixed inset-y-0 left-0 z-50 w-72 bg-white p-4 shadow-xl lg:hidden" initial={{x:-288}} animate={{x:0}} exit={{x:-288}} transition={{type:'tween',duration:.2}}><div className="flex items-center justify-between"><div className="flex items-center gap-2 text-lg font-bold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-blue-600 text-white"><Code2 size={19}/></span>DevSpace</div><button onClick={onClose} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Close navigation"><X size={20}/></button></div><div className="mt-8"><NavigationLinks onNavigate={onClose}/></div></motion.aside></>}</AnimatePresence> }
